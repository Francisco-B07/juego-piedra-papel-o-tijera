{
  "name": "juego-piedra-papel-o-tijera",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.1"
  },
  "source": "src/index.html",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build",
    "deploy": "gh-pages -d ",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}